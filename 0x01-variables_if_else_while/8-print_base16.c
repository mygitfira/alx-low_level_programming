#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
int c = 48, m = 97;
	while (c <= 57)
	{
		putchar(c);
		c++;
	}
while (m <= 102)
{
putchar(m);
	m++;
	}
	putchar(10);
return (0);
}
